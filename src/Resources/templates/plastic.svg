<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="{{ totalWidth }}" height="18" role="img" aria-label="{{ vendor }}: {{ value }}">
    <title>{{ vendor }}: {{ value }}</title>
    <linearGradient id="s" x2="0" y2="100%">
        <stop offset="0" stop-color="#fff" stop-opacity=".7"/>
        <stop offset=".1" stop-color="#aaa" stop-opacity=".1"/>
        <stop offset=".9" stop-color="#000" stop-opacity=".3"/>
        <stop offset="1" stop-color="#000" stop-opacity=".5"/>
    </linearGradient>
    <clipPath id="r">
        <rect width="{{ totalWidth }}" height="18" rx="4" fill="#fff"/>
    </clipPath>
    <g clip-path="url(#r)">
        <rect width="{{ vendorWidth }}" height="18" fill="{{ vendorColor }}"/>
        <rect x="{{ vendorWidth }}" width="{{ valueWidth }}" height="18" fill="{{ valueColor }}"/>
        <rect width="{{ totalWidth }}" height="18" fill="url(#s)"/>
    </g>
    {{ logoElement }}
    <g fill="#fff" text-anchor="middle" font-family="Verdana,Geneva,DejaVu Sans,sans-serif" text-rendering="geometricPrecision" font-size="110">
        <text aria-hidden="true" x="{{ vendorStartX }}" y="140" fill="#010101" fill-opacity=".3" transform="scale(.1)">{{ vendor }}</text>
        <text x="{{ vendorStartX }}" y="130" transform="scale(.1)">{{ vendor }}</text>
        <text aria-hidden="true" x="{{ valueStartX }}" y="140" fill="#010101" fill-opacity=".3" transform="scale(.1)">{{ value }}</text>
        <text x="{{ valueStartX }}" y="130" transform="scale(.1)">{{ value }}</text>
    </g>
</svg>